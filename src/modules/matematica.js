const PI = 3.14;

function sumar(a,b){
 return a+b;
}

function restar(a,b){
 return a-b;
}

const multiplicar = (a,b) => a*b;

const dividir = (a,b) => a/b;

module.exports = {
 PI,
 sumar,
 restar,
 multiplicar,
 dividir
};